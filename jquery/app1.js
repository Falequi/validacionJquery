// Importo el archivo milibreria.js que contiene las funciones area y perimetro
import { area } from './milibreria';
let areas = area();

let largo = 18;
let ancho = 9;

// Llamo a las funciones definidas en el archivo milibreria.js 
areas(largo, ancho);
// perimetro(largo, ancho);